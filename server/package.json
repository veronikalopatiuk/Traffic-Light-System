{
  "name": "traffic-lights-server",
  "version": "1.0.0",
  "description": "traffic lights server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon --exitcrash --inspect --delay 1500ms server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.3",
    "cross-env": "^7.0.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  }
}
